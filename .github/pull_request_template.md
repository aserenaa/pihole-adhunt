## What and why

<!-- What does this change, and what problem does it solve? -->

## Checklist

- [ ] `pnpm lint` and `pnpm test` pass
- [ ] `README.md` is updated if a command, option or behavior changed
- [ ] Changes to `src/safe.js` explain what breaks when the domain is blocked (or why unblocking is harmless)
- [ ] No HAR files, passwords, query logs, IP addresses or hostnames from a real network
